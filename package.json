{
  "name": "task-manager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "reset:backend": "npm run reset --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
